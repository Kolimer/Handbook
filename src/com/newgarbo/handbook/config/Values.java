package com.newgarbo.handbook.config;

public class Values
{
	/** Should the plugin send the MOTD to a player when they join? */
	public boolean sendMotd = false;
	
	/** Should the plugin use a custom join message? */
	public boolean customJoinMessage = false;
	
	/** The custom join message. */
	public String joinMessage = null;
}
